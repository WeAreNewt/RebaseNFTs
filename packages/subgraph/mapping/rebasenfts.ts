import { Stake } from '../generated/schema'
import { StakeCall } from '../generated/RebaseNfts/RebaseNfts'
import { log } from '@graphprotocol/graph-ts'

export function handleStake(event: StakeCall): void {
    // let stake = new Stake('123')
    
    // stake.amount = BigInt.fromI32(9999)
    // stake.timestamp = BigInt.fromI32(9999)
    // stake.save()
}

